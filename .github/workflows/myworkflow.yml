name: Print Hello World

on:
  push:

jobs:
  print:
    runs-on: macos-latest
    steps:
      - name: Print Hello
        run: echo "Hello World"

  second-job:
    runs-on: ubuntu-latest
    steps:
      - name: Greet User
        run: node index.js 